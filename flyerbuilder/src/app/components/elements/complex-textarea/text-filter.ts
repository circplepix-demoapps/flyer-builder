import { TestData } from '../../../helpers/testData';


export class TextFilter {
  text = TestData.LOREM_IPSUM;

  constructor() {
    // let textArr = each word is in the array, and each word becomes its own span.
    // This is updated when words change
      // but keep as much styling as possible.


  }
}
