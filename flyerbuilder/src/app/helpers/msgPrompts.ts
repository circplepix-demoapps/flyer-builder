
export class MsgPrompts {
   public static msg(intMsg: number) {
      let msg = '';
      switch (intMsg) {
         case 1:
            msg = 'Select an element in the canvas and try again.';
            break;
         case 2:

            break;
         case 3:

            break;
         case 4:

            break;
      }

      return msg;
   }
}